{% extends 'DualBundle:layout:base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-2">
                {{ render(path('dual_layout_sidebar')) }}
            </div>
            <div class="col">
                {% block content %}{% endblock %}
            </div>
        </div>
    </div>
{% endblock %}