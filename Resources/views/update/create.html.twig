{% extends 'DualBundle:layout:default.html.twig' %}

{% block content %}
    <h2>Create a new <em>{{ meta.name }}</em> entry</h2>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <div class="form-group">
        <input type="submit" class="btn btn-primary btn-lg" value="Create entry">
    </div>

    {{ form_end(form) }}

    {{ dump(meta) }}

{% endblock %}